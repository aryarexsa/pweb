<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <title>Login</title>
  <style>
  
    body {
      background: linear-gradient(to right, #5fffff, #e3279e); 
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .login-container {
      background: white;
      max-width: 650px;
      width: 100%;
      margin: auto;
      margin-top: 100px;
      padding: 70px;
      border-radius: 15px;
      box-shadow: 0 15px 100px rgba(0, 0, 0, 0.1);
      opacity: 0;
      animation: fadeInUp 1s forwards;
    }

    .form-title {
      text-align: center;
      margin-bottom: 30px;
      font-size: 28px;
      font-weight: 700;
      color: #333;
    }

    .form-control {
      border-radius: 10px;
      padding: 15px;
      border: 1px solid #ddd;
      font-size: 16px;
      transition: all 0.3s ease;
    }

    .form-control:focus {
      border-color: #5ffcff;
      box-shadow: 0 0 10px rgba(95, 226, 255, 0.5);
    }

    .btn-primary {
      border-radius: 30px;
      padding: 15px;
      transition: all 0.3s ease-in-out;
    }

    .btn-primary:hover {
      background-color: #7be8fe;
      transform: translateY(-3px);
    }

    footer {
      background-color: #343a40;
      color: #ddd;
      padding: 10px 0;
    }

    @keyframes fadeInUp {
      0% {
        opacity: 0;
        transform: translateY(30px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>
 
  <div class="container d-flex justify-content-center align-items-center mt-5 mb-5">
    <div class="login-container">
      <h2 class="form-title text-primary">Login</h2>
      <form action="/auth/login" method="POST">
        <div class="mb-3">
          <label for="email" class="form-label fw-semibold">Email</label>
          <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required>
        </div>
        <div class="mb-3">
          <label for="password" class="form-label fw-semibold">Password</label>
          <input type="password" class="form-control" id="password" name="password" placeholder="Enter your password" required>
        </div>
        <button type="submit" class="btn btn-primary w-100 fw-bold mt-3">Login</button>
      </form>
    </div>
  </div>

  <footer class="mt-auto">
    <div class="container text-center">
      <span>© 2024 Digital Signature App. All rights reserved.</span>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>